{
  "private": true,
  "type": "module",
  "version": "2.1.0",
  "scripts": {
    "astro": "astro",
    "build": "astro check && astro build",
    "clean": "rm -rf dist",
    "clean:deep": "yarn clean && rm -rf node_modules",
    "check": "astro check",
    "dev": "astro dev --host 0.0.0.0",
    "preview": "astro preview",
    "start": "astro dev"
  },
  "dependencies": {
    "@repo/import-test": "workspace:",
    "astro": "7.2.2",
    "common-tags": "^1.8.2",
    "sharp": "^0.35.3"
  },
  "devDependencies": {
    "@astrojs/check": "0.9.10",
    "@types/common-tags": "^1.8.4"
  }
}
